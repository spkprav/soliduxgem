require 'spec_helper'

describe Soliduxgem do
  it 'has a version number' do
    expect(Soliduxgem::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
